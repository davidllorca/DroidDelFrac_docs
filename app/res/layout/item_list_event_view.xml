<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <ImageButton
        android:id="@+id/icPerfilEventView"
        android:layout_width="@android:dimen/app_icon_size"
        android:layout_height="@android:dimen/app_icon_size"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:background="@drawable/circle"
        android:src="@drawable/ic_launcher_oval" />

    <ImageButton
        android:id="@+id/icCancelEventView"
        android:layout_width="@android:dimen/app_icon_size"
        android:layout_height="@android:dimen/app_icon_size"
        android:layout_alignParentRight="true"
        android:layout_marginLeft="24dp"
        android:background="@drawable/circle"
        android:src="@android:drawable/ic_delete" />

    <ImageButton
        android:id="@+id/icAcceptEventView"
        android:layout_width="@android:dimen/app_icon_size"
        android:layout_height="@android:dimen/app_icon_size"
        android:layout_toLeftOf="@id/icCancelEventView"
        android:background="@drawable/circle"
        android:src="@android:drawable/ic_input_add" />

    <TextView
        android:id="@+id/eventNameEventView"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_toRightOf="@id/icPerfilEventView"
        android:layout_toStartOf="@id/icAcceptEventView"
        android:gravity="center|center_vertical"
        android:text="Event name" />

</RelativeLayout>